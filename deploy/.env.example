# Copy to .env and fill in real secrets before any non-local deployment.
POSTGRES_PASSWORD=change-me-in-prod
MINIO_USER=fadstream
MINIO_PASSWORD=change-me-in-prod
JWT_SECRET=generate-a-long-random-string
